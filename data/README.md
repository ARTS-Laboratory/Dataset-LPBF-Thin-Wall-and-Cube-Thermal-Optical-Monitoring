# Data
This folder contains the experimental monitoring data. The `raw` folder contains the original acquired data, while the `processed` folder contains selectively extracted and curated data relevant to the engineering analyses, with non-relevant portions of the acquisition excluded.

